/// <reference path="../src/chartTypes.d.ts" />

export enum EventType {
    moveX,
    moveY,
    rotate,
    alpha,
    speed,
    easing,
    bpm
}
export enum NoteType {
    tap=1,
    drag=4,
    flick=3,
    hold=2
}
